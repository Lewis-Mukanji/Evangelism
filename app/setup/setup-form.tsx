"use client";
import { FormEvent,useState } from "react";
import { useRouter } from "next/navigation";
export default function SetupForm(){const router=useRouter();const [error,setError]=useState("");const [loading,setLoading]=useState(false);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setLoading(true);const f=new FormData(e.currentTarget);const r=await fetch("/api/auth/bootstrap",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({name:f.get("name"),email:f.get("email"),phone:f.get("phone"),password:f.get("password")})});const d=await r.json();if(!r.ok){setError(d.error||"Could not create account.");setLoading(false);return;}router.push("/dashboard");router.refresh();}return <form onSubmit={submit}><div className="field"><label>Your full name</label><input required name="name"/></div><div className="field"><label>Email address</label><input required type="email" name="email"/></div><div className="field"><label>Phone number</label><input name="phone"/></div><div className="field"><label>Create password</label><input required minLength={8} type="password" name="password"/></div>{error&&<p className="error">{error}</p>}<button className="button" style={{width:"100%"}} disabled={loading}>{loading?"Creating…":"Create administrator"}</button></form>;}
