"use client";
import { FormEvent,useState } from "react";
import { useRouter } from "next/navigation";
export default function ParticipantForm(){const router=useRouter();const [message,setMessage]=useState("");const [error,setError]=useState("");async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setError("");setMessage("");const f=new FormData(e.currentTarget);const r=await fetch("/api/admin/participants",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(Object.fromEntries(f))});const d=await r.json();if(!r.ok){setError(d.error);return;}setMessage("Participant registered. Share their email and temporary password securely.");e.currentTarget.reset();router.refresh();}return <form onSubmit={submit} style={{display:"grid",gridTemplateColumns:"repeat(2,minmax(0,1fr))",gap:12}}><div className="field"><label>Full name</label><input required name="name"/></div><div className="field"><label>Email</label><input required type="email" name="email"/></div><div className="field"><label>Phone</label><input name="phone"/></div><div className="field"><label>Temporary password</label><input required minLength={8} type="password" name="password"/></div><div className="field"><label>Date of conversion</label><input type="date" name="conversionDate"/></div><div className="field"><label>Location / church branch</label><input name="location"/></div>{error&&<p className="error">{error}</p>}{message&&<p style={{color:"#28743e"}}>{message}</p>}<button className="button" style={{width:"fit-content"}}>Register participant</button></form>;}
